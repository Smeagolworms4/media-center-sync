{
	"name": "@mcs/shared",
	"version": "0.1.0",
	"private": true,
	"description": "Contrats partagés entre l'api et l'interface — énumérations, formes d'échange, clés d'erreur",
	"main": "dist/index.js",
	"types": "dist/index.d.ts",
	"scripts": {
		"build": "tsc -p tsconfig.json",
		"typecheck": "tsc -p tsconfig.json --noEmit"
	}
}
